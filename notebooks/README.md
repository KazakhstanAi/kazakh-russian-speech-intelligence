# Notebooks

VAIS Voice notebooks may explore benchmark coverage, paired regressions, latency distributions, reviewer agreement and audio robustness; they must not initiate external calls without explicit sandbox authorisation.

Notebooks are for exploration and communication. Use only approved or synthetic fixtures in committed notebooks, clear private outputs, identify the manifest version, and set seeds. Reusable audio, modelling, and evaluation logic belongs in `src/` with tests.
